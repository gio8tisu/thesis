\ProvidesPackage{preamble}

\usepackage[english]{babel}
\usepackage[utf8]{inputenc}

\usepackage{graphicx}
\usepackage{tikz}
\usetikzlibrary{arrows}
\usepackage{xcolor}

\usepackage[inline]{enumitem}

\usepackage{caption}
\usepackage{subcaption}

\usepackage[
backend=biber,
style=authoryear,
citestyle=authoryear
]{biblatex}\addbibresource{refs.bib}
\usepackage{url}
\usepackage{hyperref}
\hypersetup{
    colorlinks,
    citecolor=black,
    filecolor=black,
    linkcolor=black,
    urlcolor=black
}

\usepackage{csquotes}
\usepackage[acronym,nomain,nonumberlist]{glossaries}
\loadglsentries{acronyms.tex}
\makeglossaries

\usepackage{float}
\usepackage{verbatim}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{array}
\usepackage{booktabs}
\usepackage{fancyhdr}
\usepackage{parskip}
\usepackage{titlesec}

\usepackage{subfiles}

\usepackage{lipsum}

%% Page formatting
\setlength{\parskip}{5mm}
\setlength{\parindent}{5mm}
\setlength{\footskip}{60pt}
\setlength{\headheight}{15pt}
\usepackage[a4paper,top=3cm, bottom=3.2cm, inner=3cm, outer=2cm, footnotesep=1cm, heightrounded]{geometry}

%% Strart new page for each section iff there is something written in the last one.
\newcommand{\sectionbreak}{\cleardoublepage}

%% Page header and footer (E/O (even/odd), L/C/R (left/center/right) and H/F (header/footer))
\fancyhf{}
%\fancyhead[ER]{Degree thesis}
\fancyhead[ER]{\rightmark}
\fancyhead[OL]{Data-driven CM to H\&E transformation}
\fancyhf[ELH, ORH]{\thepage}
%\fancyfoot[C]{}
\fancyfoot[EL, OR]{
\includegraphics[scale=0.1]{logo_telecos_2018}}
\pagestyle{fancy}
\raggedbottom

%% \SetWatermarkText{\hspace{9mm}\transparent{0.15}\includegraphics[scale=2.5]{logo_telecos_2018}}
%% \SetWatermarkAngle{0}
%% \SetWatermarkLightness{1}


%% Custom commands
\newcommand{\cycleGAN}[2]{G_{#1 \rightarrow #2}}
\newcommand{\tensor}[1]{\mathbf{#1}}
\newcommand{\R}{\mathbb{R}}
\DeclareMathOperator*{\argmax}{arg\!\max}
\DeclareMathOperator*{\argmin}{arg\!\min}
\newcommand{\iteration}[2]{#1^{(#2)}}
